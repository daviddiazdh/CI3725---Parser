{
    int a, b, c, x;
    bool d, e, f;
    function[..3] A, B;
    a := b * 4 + 2;
    
    d := true;
    A := A.25, 3, 3, 4;
    A := A(4:2)(1:1);

    {
        int y, q;
        y := q
    };

    print "Hola" + 2;

    if !!!!!d == false -->
        b := 2
    fi
}